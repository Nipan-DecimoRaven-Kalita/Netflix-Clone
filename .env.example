# MongoDB Atlas - Create free M0 cluster, whitelist 0.0.0.0/0, copy connection string
MONGO_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/netflix-clone?retryWrites=true&w=majority

# OMDB API (provided)
OMDB_API_KEY=278e1ea3
OMDB_BASE_URL=https://www.omdbapi.com/

# JWT (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# Server
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
